# Patronage

Cozy Renaissance-Italy city builder. Full design spec: @docs/design-doc.md — read it before implementing any game system (resources, commissions, buildings, worker allocation, artists, etc.).

## Non-negotiable design constraints

From the doc's "Key Design Principles" — flag it if a change would violate one of these rather than just implementing it:

- No citizen pathfinding, no granular supply chains — service/material buildings are capacity/threshold checks, not simulations. Materials are supplier capacity, never a stockpile.
- All art is commissioned (once Phase 8 lands): every artwork has a requester, a name, and stakes. Requesters (Church, noble families) carry a 0–100 favor meter moved only by player decisions — completions up, declined/expired offers down, **never time decay**. Consequences stay faction-scoped (that faction's offer rate and grandeur), with one sanctioned exception: a first slide into Affronted fires a one-time denunciation prestige hit. Favor is never a top-bar resource.
- Exactly 3 headline resources (Florins, Inspiration, Prestige) + Population as status. Don't add a new top-bar resource without a real player decision behind it.
- Every building either unlocks something or passively boosts something — never requires active management.
- Spatial effects are soft: plaza proximity is a flat bonus, never a hard in/out radius.
- Renaissance = prestige milestone soft ending; play continues afterward.

- After implementing a phase in the design doc, move it to the done section. When other features are complete, mark them as *(built)*. 
- Commit messages are one line. Add a body only when the change genuinely can't be understood without one — the docs carry the reasoning, not the git log.

## Current state vs. doc phases

Phases 0–12 done (placement, tick loop, workers, artists + ranks, artworks, supplier capacity gating, commissions, work display, plaza connectivity, artist training, Renaissance milestone) plus graphics G1–G4 and most of G5 (seeded water layer — five map archetypes from dry to coastal — + stone bridges; see the doc's water pass). Decorative citizens scale with population (exact when low/countable, damped toward a cap beyond — `crowdSize` in `app/game/crowd.ts`) and render as thin-instance batches (`createThinInstanceFigureFactory` in `app/game/render/citizenFigures.ts`, 15 draw calls for any crowd size); count is their only sim coupling — still cosmetic random-walkers, no pathfinding. Phase 9 work display: typed display slots on buildings/plazas (`app/game/display.ts`), marble statues on plinths + framed paintings on easels (`app/game/render/displayArt.ts`), displayed works trickle inspiration/prestige + boost their host, click-a-building `DisplayPanel` + Gallery "Display at…". Phase 11 training: continuous XP (`app/game/artists.ts` `progressArtworks` + `XP_RATES`) — passive practice every month for artists in an active workshop, multiplied when a higher-ranked workshop-mate teaches, plus the completion bonus. Phase 12 Renaissance milestone (July 2026): four gates derived live from persisted state (`renaissanceProgress` in `app/game/renaissance.ts`, knobs `RENAISSANCE_PRESTIGE`/`WONDER_PRESTIGE`/`RENAISSANCE_NOBLE_HOUSES` in `constants.ts`) — prestige ≥ 500, a Master-rank artist, a Wonder on display (a displayed work ≥ 15 quality), works for the Church + 2 noble houses (the pre-factions favor stand-in; guild requesters ignored) — one-shot celebration card (`ui/RenaissanceCard.tsx`, persisted `renaissanceReached` flag, no migration) + the checklist on the prestige chip's hover tooltip; play continues (soft ending). Next up: Later/stretch items (factions, map resources, zoning, …). Roads and houses are player-placed individually — that's intentional current scope (zoning is a later phase; auto-roads were cut); all five road variants drag at 45° diagonals — paved roads plus dirt path and stone bridge (diagonal/cardinal crossings swap the ribbon cell for a mottled junction pad — stone for paved, rimless earth for dirt; a hexagonal plate where a street passes through the cell, the plain ribbon-strip shape at lane-side contacts — and paved ribbons carry a √2-corrected slab texture; see the doc's Diagonal streets). Buildings rotate in 45° steps (R cycles 8; `Tile.rotation` 4–7 = quarter + 45°, claiming a diamond cell mask via `footprintMask` in `app/game/buildings.ts` — never treat a diagonal footprint as its bounding box), and holding Shift while placing snaps the ghost flush to a nearby road, auto-facing it (`app/game/roadSnap.ts`); row-house blending and colonnade extend are deliberately skipped at 45° (see the doc's Snap-to-road section). A main menu fronts the boot (`app/game/ui/MainMenu.tsx`: continue, new game with optional seed or archetype pick via `seedForArchetype`, demo tour — `?demo` is no longer dev-only); Settings → Main Menu reloads back to it, and the save only hydrates on Continue. Numbers pass (July 2026): XP runs on a ×100 scale (`RANK_XP` 400–4000, all `XP_RATES` gains integers; save v7 migrates old artists ×100) and the roster shows founder progress ("640 / 900 XP", `nextRankXp` in `app/game/artists.ts`); the first building-effects wave is wired — cottage/townhouse rents (2ƒ/5ƒ), chapel passive amenity (+10, workerless), palazzo `housing: 12`, cathedral consecration lump (`prestigeOnBuild: 25`, paid in `placeTiles`), decoration inspiration trickles (per-cell rates on linear pieces), and the bell tower as a secondary hub — `connectivity.ts` now derives `PLAZA_IDS` from the `isHub` metadata flag instead of hard-coding plaza ids. Factions slice 1 (July 2026): the requester pool is now gated by patron admission (`requesterPool` in `app/game/commissions.ts` — Chapel or Cathedral seats the Church, each Palazzo installs the next noble house in table order; guild requesters removed) and each patron carries persisted 0–100 favor (`favor` in the store; reads default `FAVOR_START` 50; save v8 seeds old saves from per-requester completed works). Favor moves only on decisions — `FAVOR_PER_WORK` +8, `FAVOR_SLIGHT` −5 per declined (`declineCommission`) or expired open offer — with rungs at 60/75/90 (`favorRung`) multiplying offer duration/florins/prestige by `FAVOR_GRANDEUR` up to 2× (the Church's rungs 2–3 need a standing cathedral); below 35 (`favorTier` cooled) offers skip half the time and force rung 0, below 15 (affronted) skip 75%, and the ≥15→<15 crossing fires a one-time denunciation (−15 prestige, clamped ≥0, alert card; re-arms on recovery). Pacing rebalanced to rare-but-rich: `COMMISSION_OFFER_CHANCE` 0.08, `FLORINS_PER_PRESTIGE` 40, new `COMMISSION_PRESTIGE_SCALE` 1.5; Church offers draw `CHURCH_TITLES` (artists.ts). UI: per-patron crest banner top-right (`FactionBanner.tsx`), persistent arrival + denunciation cards (`OfferAlert.tsx`, transient `offerAlert`/`denounceAlert` store fields), Decline + "— Nth work" on the commissions panel. Future slices: taste jitter, seed-shuffled installs, rivalry pairs, signature chains. Late-game money rebalance (July 2026): florins stay the constraint resource while prestige keeps the full reward curve — house rent scales with occupancy (`min(1, population/housing)` in `app/game/tick.ts`, replacing flat-per-house rent), duplicate non-housing florin generators (e.g. a second Market) get geometric diminishing returns (`INCOME_DIMINISHING_RETURNS`), and commission florin rewards are compressed against artist rank (`FLORIN_RANK_COMPRESSION` in `app/game/commissions.ts`) while commission prestige is untouched. No upkeep, no new sink — the existing big-ticket build-out is the outlet, and a second wave deepens that outlet as the city grows: duplicate workshops/suppliers/services (`costEscalates` in `app/game/buildings.ts` — `artist`/`materials`/`service` types) cost progressively more (`COST_ESCALATION`, `escalatedCost`/`buildOrderRank`), priced live off the current tile map with no persisted counter or save migration; raze salvage (`getRazeSalvage`) tracks the escalated price actually paid rather than the flat base cost. Market Stall (July 2026): the first `placesOnRoads` building — a workerless 1×1 street stall placed onto plain cardinal road cells or a plaza's outer ring (non-origin perimeter cells, mask-based `isPlazaRimCell` so stalls never erode inward — plinth slots are all interior; overwrites the cell via the shared `checkCell` rule in `placementRules.ts`; blocked on bridges, diagonal ribbons, plaza interiors) and conducting plaza connectivity at road cost (`ROAD_OVERLAY_IDS` in `connectivity.ts`, derived from the same flag) so 1-wide paths never sever; its small florin/amenity output scales by the per-building `connectionBonus` metadata field (stall 1.0; everything else defaults to the global 0.25 via `connectionBonusOf`) — and, since the foot-traffic pass (July 2026), the stall's bonus is real traffic, not a flat sensitivity: `footTraffic` metadata flag → `boost = 1 + connectionBonus × hubStrength × bustle × catchment` (`app/game/traffic.ts`: `bustle` = `crowdCurve(population)/BUSTLE_FULL` — the visible crowd's curve, extracted from `crowdSize` in `crowd.ts`; `catchment` = housing capacity within `CATCHMENT_REACH` network cells / `CATCHMENT_FULL`, WeakMap-memoized by tiles identity like connectivity; knobs in `constants.ts`; both factors monotonic non-decreasing — principle 6 — with `traffic.check.ts` asserting it; threaded through `tick.ts` `plazaBoost`, `computeCityMetrics` (new trailing `population` param, 0 = muted), and the tooltip's "Foot traffic: +N%" line; all derived, no save migration). Its model is the kit stall reshaped one-sided (`scripts/make-stall-side.py` → `stall-side-*.glb`: ridge slid back, long awning slope presenting +X to the street); the Market building keeps the symmetric booths. Sandstone residences (July 2026): cottage/townhouse facades read as stone — five canvas-drawn patterns, weighted by repetition (`STONE_TINTS` in `app/game/render/wallTexture.ts`) replace the cream/pink/sand/white stucco spread in `FACADE_PALETTES.residential`, riding the existing position-hashed tint path (`getTintedPair` puts the DynamicTexture on the tint clone, so the base `stucco` material, its pinned #dcd0b6 average, and every non-residential `proc:block` ref are untouched); `proc:gable-end` gained planar UVs so the courses continue up the gable. Batch-1 fittings generated (July 2026): the artist-brief pieces built in code instead of commissioned (`proc:surround-rect`/`surround-arch`/`door-frame`/`door-leaf`/`arch-bay` in `proceduralPieces.ts` — pietra-serena boxes + 6–8-facet voussoir wedge fans, one `stone`/`wood` material each so `Part.tint` recolors whole fittings): every house window wears a real stone surround around its reveal + glazed leaf and house doors are frame + planked leaf (`windowOn`/`houseFront`), the palazzo's piano nobile got nine arched surrounds (`archWindow`), and `wall-arch.glb` retired to 0 refs (`TINT_COLORS.reveal` darkened to #453d33 so shutterless reveals survive direct sun; `proc:arch-bay` serves the palazzo loggia and colonnade — the cathedral's verde blind arcade was replaced by window rows in the marble pass below). The commission (docs/artist-brief.md) now starts at batch 3 — dome/ivy, the genuinely organic pieces (the bifora and rose joined the generated set, July 2026); see docs/procedural-pieces.md. Marble pass (July 2026): the bell tower and cathedral went panel-free onto a hand-drawn white-marble inlay texture (`campanile` in `render/wallTexture.ts` — green runs vertically as pilasters/panel edges, hairline horizontal courses, rose slivers; STONE_TINTS membership routes it, no palette) — the campanile is a rebuilt 5-storey slim shaft with `archWindow`s (now scale-parameterized) growing up to a 4-face belfry under a projecting crown, while the cathedral hangs the marble as a Santa Croce **screen facade** (thin front slabs + pediment via `gableRoof`'s new `tint` override, plus gable-end wedges capping the aisle slopes; the front wears `screen` — the campanile language densified into the SMN panel grid with alternating verde/rose lozenges) over brown rubble flanks (`flank` in wallTexture.ts; the shell is unit columns — `proc:block@1x3` nave at 2.5 tall, `@1x2` aisles at 1.75 — so courses neither stretch nor mismatch) carrying a five-bay window rhythm: arched clerestories per side plus arched-over-rectangular rows on the aisles (`windowOn` gained a `wall` param like `archWindow`'s). Landmark portals (July 2026): `proc:portal-frame`/`proc:portal-leaf` — voussoir-arched stone frame with a recessed stone tympanum in the lunette + rectangular double doors of raised bronze panels, 2 wide × 3 tall (self-contained, no reveal part; `bronze` material, aged grey-green) — via `portalOn()` on the cathedral's three west portals (center 1.15×, sides 0.85×) and the bell tower base (0.75×); the future Town Hall inherits it. Panel-free pass (July 2026): the kit's wall panels hit 0 refs — every opening on every building (workshops, palazzo, chapel, suppliers, tavern, bakery) is a generated fitting via the manifest helper `doorOn()` (frame+leaf on any face; `houseFront` rides it) beside the existing `windowOn`/`archWindow`/`portalOn`; every former round-window site is an `archWindow` (a generated `proc:oculus` ring was tried and dropped — read as portholes; `archWindow` gained an optional surround-only `tint` param instead), so the chapel wears the portal at 0.85× + verde-tinted arch surrounds (sides + a small gable lancet) as its green trim (the `mint` texture tint, `make-mint-quoins.py`, `colormap-mint*.png` deleted; `wall-door.glb`/`wall-window-shutters.glb` stay on disk only as `make-plain-openings.py` inputs), the palazzo's top floor runs the piano nobile's arches a size down (0.9× at yOpen 2.35), its loggia entrance is a quiet `doorOn` (a portal there fought the arcade — portals stay landmark language), and the three suppliers were rebuilt bigger (sheds 1.25–1.35 kit, yards inside ±0.95 so the shed drives the fit; footprints stay 4×4 — no save impact). Texture pass (July 2026): every facade palette entry is now a `STONE_TINTS` pattern — workshops brick-forward, suppliers rubble, services plaster, civic a new pale-warm dressed-ashlar `civic` pattern (palazzo via the city palette, chapel walls tint it directly) — retiring the flat cream/sand/white/ochre stucco tints; stone textures gained a real desaturated twin (`getStoneTexture(id, scene, true)`, pixel-lerped like `desaturate()`) since non-housing buildings render inactive. Textured walls must keep unit-scale faces (courses stretch otherwise): storeys are one `proc:block@1xN` (single continuous AO ramp — no per-storey dark seam) and plans are unit columns — the palazzo upper block is four `@1x2` columns (wing nudged +0.005x off the main block so differently-ramped faces never share a plane), the chapel nave two unit-depth columns, the sculptor bay+head-house one `@1x2` at y-scale 0.775, the bell tower shaft one `@1x5`; gable ends on textured civic buildings tint flat `"stone"` (their planar UVs bake the 0.6 roof squash — textured courses would jump size at the eave). Glazed casements + flush fittings (July 2026): `proc:shutter` (id kept, so every ref rode along) is now a glazed leaf — slate sky-glass in a 2×3 pane grid (centre mullion + two transoms, per-pane brightness sparkle) inside a thin casement frame, two hues carried as coloured vertex tints over one white `glazing` material (`GLASS`/`MUNTIN`/`PANE_SHADES` knobs in `proceduralPieces.ts`; `shadedBox` accepts `number | Color4`; known ceiling: vertex tints don't desaturate with inactive buildings — subtle, the glass is near-grey) — and every window/door fitting was pulled near-flush to the wall after the surrounds read as boxes standing off it: piece depths `WIN_T` 0.014/`WIN_SILL_T` 0.022 (windows), `DOOR_T` 0.02 (renamed from `FIT_T`, threshold/lintel slimmed with it), the manifest depth stack compressed (`REVEAL_PLANE` 0.49, `SHUTTER_BACK` 0.006, `SURROUND_OUT`/`DOOR_OUT` derived from the piece depths so frame backs kiss the wall at 0.5005) with the door leaf sunk so it stays recessed behind the slimmer frame; landmark portals deliberately keep their heavier depth; `proceduralPieces.check.ts` pins the slimmed envelopes. Bifora pass (July 2026): `proc:bifora` — the two-light window from the Giotto's-campanile reference (twin 4-facet arched lights on a colonnette, apexes poking into the grand arch's lunette, octagonal roundel boss in the spandrel; inner order recessed a hair behind the frame planes) — via `biforaWindow()` (archWindow's contract; reveal covers the full lunette, so the outer voussoir band is deep 0.065 to keep the reveal's corners inside the *faceted* ring; deliberately **no glazed leaves** — the lights are dark voids): palazzo piano nobile went four bifora bays per long face + the three side windows (five single arches read wall-to-wall at bifora width), the bell tower runs one per face per storey at a uniform 0.95, and the cathedral west front carries two over the center portal (replacing the single arched slot) + two high on each aisle front — which surfaced that the aisle marble wedges' inner triangle tails cross the nave zone, fixed by floating the nave screen slab 0.01 proud (`CATH_NAVE` 2.05 vs `CATH_FRONT` 2.04; nave portal + bifore ride the new plane, wedge tails hide behind it). Verde trim rides `TINT_COLORS.verdeLight` (#6b878e — a blue-leaning multiplier because the warm SURROUND base eats blue; plain `verde` rendered near-black on fittings, and a neutral green multiplier rendered yellow-olive) on all bifore + the cathedral/bell-tower portal *frames* via `portalOn`'s new trailing `tint` param (surround only — the bronze door leaves keep their color). Arch glazing (July 2026): `proc:arch-leaf` — diamond leaded lattice filling the whole arch to the lunette apex (±45° bars sample-clipped to the convex rect-under-half-disc outline, tips tucked under the stiles/spring-rail/wood arc ring; centre mullion + two transoms + springline rail below; `LATTICE_PITCH`/`LATTICE_W` knobs, pitch kept chunky to match the kit's detail level) replaces the rect casement leaf on every `archWindow` — same authored contract/depth as `proc:shutter` (`meshFrom`/`wedge` now take `number | Color4` shades so lattice and lunette glass ride the white `glazing` material); biforas stay unglazed dark voids. Rose window (July 2026): `proc:rose` (16-facet verde-tintable stone annulus, voussoir shades + recessed inner step) + `proc:rose-glass` (glass disc with wheel tracery — 12 spokes, hub, two concentric rings; sector sparkle muted to 40% of PANE_SHADES) on the cathedral west front via `roseWindow()`, replacing the paired nave bifore over the grand portal (aisle bifore stay). The one CENTERED piece pair (position = circle centre, not base-at-0 — pinned in the check file): `roseWindow`'s trailing `sy` param pre-stretches y so the circle survives a prefab `scaleY` squash (cathedral passes 1/0.71 — exact, since stretch-fit `scaleY = min(scaleX, scaleZ) × 0.71`). Sagrato podium (July 2026): a two-step stone platform across the cathedral's three west portals, two plain `proc:block` slabs in the prefab (+X bbox grows to 2.24 — ~4% stretch-fit shrink, imperceptible, `fit` stays 0.95). Register cornices, pilaster capitals, and a pediment apex cross were tried in the same pass and rejected on taste; if a ridge ornament returns, note the colmo cap tops ~0.09 above the gable apex and overhangs the facade (to x 2.2 here) — a finial must rise well clear or it drowns in the cap. Stone trim pass (July 2026): every verde/verdeLight fitting tint retired — cathedral portals/bifore/rose ring, bell tower, and chapel surrounds are plain SURROUND stone again, verde survives only inside the wall textures (orphaned `TINT_COLORS` entries deleted; the multiply-tint lesson — plain verde lands near-black, neutral green goes yellow-olive, the working value was blue-leaning #6b878e — is a comment where they lived) — and the west rose moved up to yCenter 2.0 (ring top ~2.44, just under the 2.5 slab line). Marble redesign (July 2026, picked off a five-way concept board): the old hairline inlay averaged into a grey wash at the 40–90px a storey face fills on screen, so both drawers went solid-field — `drawCampanile` is Giotto polychrome (three framed panel bays per storey, verde outer / rose centre with white hexagon medallions; the centre bay hides behind each storey's bifora, which is why the green is in the outer bays — the inverted first cut read pink) and `drawScreen` is San Miniato language (five-arch blind arcade with alternating solid verde tympana under circle-in-square intarsia, ~4× the old line weight); the cathedral pediment + aisle wedges left `campanile` for a new quiet `marble` drawer (field + hairline courses only — figures stripe across the gable's per-unit u tiling and get cut by the wedge slope); the three marble drawers render at 512 (`TINT_SIZE` in `wallTexture.ts` — hard-edged geometry showed its texels at the shared 256; the course/blob drawers stay at 256, their px-tuned joints/gaps would thin at 512). Light brick pass (July 2026): the marble redesign retired — one pale Roman-brick drawer (`drawLightBrick` in `wallTexture.ts`, the housing brick recipe a few steps paler; the polychrome drawers, VERDE/ROSE, and `TINT_SIZE` deleted with it) — no green remains on the cathedral or bell tower; verde now survives nowhere on those two landmarks (the flanks stay brown rubble). Religious brick family (July 2026): the three alias tint ids (`campanile`/`screen`/`marble`) collapsed into one `lightbrick`, and the chapel's nave columns left `civic` for it — the two pale families now split by *pattern*, religious = light brick (cathedral front/pediment/wedges, campanile, chapel) vs civic = dressed ashlar (palazzo, future Town Hall); category differentiation otherwise deliberately stays on the three existing channels (palette mix, shape grammar, activity props — smoke/banners/yards), and noble-family identity (stemma heraldry, rustication) waits for the factions / housing-tier phases. Chapel rose (July 2026): the front gable lancet is now a small `roseWindow` (0.4× vs the cathedral's 0.9), tying the two churches visually; two traps found in the placing — the gable-end piece is ±0.03 *centered* on the wall plane with thickness riding the ridge scale, so a chapel-roof-scale-2 gable's outer face is at 1.06, not the 1.03 the old comment claimed (the lancet had been authored at 1.03 and sat *buried inside the gable*, invisible, since the panel-free pass), and `roseWindow`'s `sy` stays the plain reciprocal only because the chapel's min fit axis (x) is also its front's horizontal — verified against the placed building's actual root scaling (1.613/1.016/1.726), not derived from bbox guesses.

Layout: `app/game/` = sim logic (building defs, tick loop, worker allocation), `app/game/render/` = Babylon rendering, `app/game/ui/` = DOM overlay UI, `app/stores/useGameStore.ts` = Zustand store. Check there before assuming a system exists.
